{% extends "_layouts/grids/_base2" %}

{% set gridDefaults = {
    classes: {
        wrapperBase : 'relative w-full grid grid-cols-1 justify-center items-center gap-10',
        gridBase    : 'grid grid-cols-1 gap-8 lg:gap-14',
        content1Base: 'w-full',
        content2Base: 'w-full grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-8 lg:gap-16 xl:gap-20',
    }
} %}
  
{% set props = {
    classes:config is defined and config is iterable ? gridDefaults.classes | merge(blockConfig) : gridDefaults.classes
} %}