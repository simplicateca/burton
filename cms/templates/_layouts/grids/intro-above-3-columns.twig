{% extends "_layouts/grids/_base2" %}

{% set gridDefaults = {
    classes: {
        wrapperBase : 'relative w-full grid grid-cols-1 justify-center items-center gap-10',
        gridBase    : 'grid grid-cols-1 gap-8 lg:gap-14',
        content1Base: 'w-full',
        content2Base: 'w-full grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 lg:gap-12 xl:gap-16',
    }
} %}
  
{% set props = {
    classes: blockConfig is defined and blockConfig is iterable ? gridDefaults.classes | merge(blockConfig) : gridDefaults.classes
} %}