{% extends "_layouts/grids/_base2" %}

{% set gridDefaults = {
    classes: {
        wrapper     : 'grid grid-cols-1 gap-12',
        contentCols : 'grid grid-cols-1 gap-12',
        content1Base: 'max-w-5xl mx-auto',
        content2Base: 'w-full grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-8 lg:gap-12',
        content1    : '',
        content2    : '',
    }
} %}
  
{% set props = {
    classes: blockConfig is defined and blockConfig is iterable ? gridDefaults.classes | merge(blockConfig) : gridDefaults.classes
} %}