{% extends "_layouts/grids/_base2" %}

{% set gridDefaults = {
    classes: {
        wrapper      : 'grid grid-cols-1 gap-12',
        contentCols  : 'w-full flex flex-col xl:flex-row-reverse gap-12',
        content1Base : 'w-full xl:w-1/4',
        content2Base : 'w-full xl:w-3/4 grid grid-cols-1 md:grid-cols-2 gap-12',
        content1     : '',
        content2     : '',
        content1Inner: 'xl:sticky top-20 w-full',
    }
} %}
  
{% set props = {
    classes: blockConfig is defined and blockConfig is iterable ? gridDefaults.classes | merge(blockConfig) : gridDefaults.classes
} %}