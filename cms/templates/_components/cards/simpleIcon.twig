{% import "_macros/richtext" as richtext %}

{% set htag    = htag    ??? 'h3' %}
{% set title   = title   ??? entry.title ??? entry.headline ??? null %}
{% set text    = text    ??? entry.summary ??? entry.dek ??? entry.text ??? '' %}
{% set url     = url     ??? entry.url ??? null %}
{% set handle  = handle  ??? entry.section.handle ??? null %}
{% set images  = images  ??? entry.images.all() ??? null %}

<div class="flex flex-col gap-2">
    <div>
        <div class="aspect-square h-16 bg-slate-200">&nbsp;</div>
    </div>

    {% block title %}
        {%- if title -%}
            <{{htag}} class="font-body not-italic text-base mb-0 uppercase font-bold">{{title}}</{{htag}}>
        {%- endif -%}
    {% endblock %}

    {% block summary %}
        {{ richtext.prose( text, 'sm' ) }}
    {% endblock %}
</div>