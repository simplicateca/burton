{% set menu    = menu  ?? "primary" %}
<div class="flex flex-grow items-start w-full items-center">
    {{ include( "_components/navigation/mobile", { menu: menu }, withContext = false ) }}
    
    {{ include( "_components/navigation/menu",   {
        menu: menu,
        class: "items-center ml-2 hidden md:flex",
        liClass:'font-display',
        aClass: 'nav-link text-2xl ml-8 text-gray-500 hover:text-gray-700',
        activeClass:'nav-active text-gray-700'        
    }, withContext = false ) }}
</div>