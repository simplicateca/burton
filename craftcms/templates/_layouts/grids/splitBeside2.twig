{% extends "_layouts/grids/_base2Cell" %}

{% set _grid = {
    gridBase  : 'w-full relative flex flex-col lg:flex-row gap-10 lg:gap-16 xl:gap-24',
    gridCell1 : 'w-full h-full lg:w-1/2',
    gridCell2 : 'w-full h-full lg:w-1/2',
    gridInner2: 'grid grid-cols-2 gap-8 lg:gap-12 xl:gap-18'
} %}

{% set _grid = settings is defined and settings is iterable ? _grid | merge(settings) : _grid %}