{% extends "_layouts/grids/_base2Cell" %}

{% set _grid = {
    gridBase  : 'w-full',
    gridCell1 : 'w-full relative z-30',
    gridInner1: 'mx-auto w-full @container',
    gridCell2 : 'absolute top-0 left-0 bottom-0 right-0 overflow-hidden z-10'
} %}

{% set _grid = settings is defined and settings is iterable ? _grid | merge(settings) : _grid %}
