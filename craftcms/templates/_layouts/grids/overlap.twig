{% extends "_layouts/grids/_base2Cell" %}

{% set gridDefaults = {
    classes: {
        gridBase  : 'w-full flex flex-col gap-10 lg:gap-16 xl:gap-24',
        gridCell1 : 'w-full relative z-30',
        gridCell2 : 'absolute top-0 left-0 bottom-0 right-0 overflow-hidden'
    }
} %}

{% set props = {
    classes:settings is defined and settings is iterable ? gridDefaults.classes | merge(settings) : gridDefaults.classes
} %}