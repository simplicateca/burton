{% extends "_layouts/grids/_base1Cell" %}

{% set _grid = {
    gridBase  : 'w-full relative',
    gridCell1 : 'w-full'
} %}

{% set _grid = settings is defined and settings is iterable ? _grid | merge(settings) : _grid %}