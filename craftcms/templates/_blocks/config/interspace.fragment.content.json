[
    {
        "label": "Use Fragment Settings",
        "value": "FROMFRAGMENT"
    }

    {% for item in source('_blocks/config/interspace.content.json') | json_decode() %}
    ,{{ item | json_encode() | raw }}
    {% endfor %}
]
