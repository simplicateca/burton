{## What are we supposed to be rendering?
 ------------------------------------------------------------------------------------ ##}
{% set items       = items    ??? null %}
{% set settings    = settings ??? null %}
{% set cardLayout  = 'detail.accordion' %}
{% set headlineTag = settings.itemHeadlineTag ??? 'h3' %}

{% embed '_components/accordion/base' with {
    items   : items,
    settings: settings,
    card    : cardLayout,
    hTag    : headlineTag,
} %}
    {% block panelContent %}
        {% from "_cards/base" import render as renderCard %}
        {{ renderCard( panel, card, settings ) }}
    {% endblock %}

    {% block panelTitle %}
        {% from "_cards/base" import title as cardTitle %}
        {{ cardTitle( panel, card, settings ) }}
    {% endblock %}

    {% block panelOpenClass %}
        p-6 bg-white
    {% endblock %}
{% endembed %}