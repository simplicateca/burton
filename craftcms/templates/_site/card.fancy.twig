{% extends ["_site/card", "_core/card"] %}

{# {% block content %}
    {% block fancy_card %}
        <div class="w-full relative @container py-10 @xl:py-20">
            <div class="relative z-10 flex flex-col h-full">
                {{ block('fancy_card_image') }}
                {{ block('fancy_card_text') }}
                {{ block('fancy_card_action') }}
            </div>

            {{ include( "_site/partials/blob" ) }}
        </div>
    {% endblock %}
{% endblock %}


{% block fancy_card_image %}
    {% block card_image %}
        {% set cardImage %}{{ parent() }}{% endset %}
        {% if cardImage %}<div class="image">{{ cardImage }}</div>{% endif %}
    {% endblock %}
{% endblock %}


{% block fancy_card_text %}
    <div class="p-2 @md:p-4 flex flex-grow flex-col gap-2 bg-theme-contrast-light">
        {% set cardHeaderClass = 'mb-0' %}
        {{ block('card_headline') }}

        {{ block('card_summary') }}

        {{ block('card_after_summary') is defined ? block( 'card_after_summary' ) }}
    </div>
{% endblock %}


{% block fancy_card_action %}
    <div class="px-2 @md:px-4">
        {% block action %}
            {{ block('card_action') }}
        {% endblock %}
    </div>
{% endblock %} #}