[
    {
        "label": "Use Fragment Settings",
        "value": "FROMFRAGMENT"
    }

    {% for item in source('_site/settings/interspace.content.json') | json_decode() %}
    ,{{ item | json_encode() | raw }}
    {% endfor %}
]
