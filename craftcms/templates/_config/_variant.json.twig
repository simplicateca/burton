{## SelectPlus Variant Field Configuration
{---------------------------------------------------------------------------------------}
 !! OUTPUT FORMAT should be a **PLAINTEXT JSON ARRAY**  i.e.  [{...},{...},{...},{...}]

    `type`  is the **handle** of the EntryType this SelectPlus field is attached to.
    `field` is the **handle** of this SelectPlus field.

    Other fields are available: https://github.com/simplicateca/craft-selectplus-field

    When `type` is null, it's because Craft is looking for all possible options to use
    in a list of conditional field settings.
{-------------------------------------------------------------------------------------##}
{%- set type    = type ??? '' | lower -%}
{%- set element = element ??? '' | lower -%}
{%- set field   = field ??? null -%}


{## [Type] Addon → http://localhost:8000/cms/settings/entry-types/20
{-------------------------------------------------------------------------------------##}
{{- 'addon'     == type ? include( '_config/addon.variant.json'   ) : null -}}


{## [Type] Article → http://localhost:8000/cms/settings/entry-types/27
{-------------------------------------------------------------------------------------##}
{{- 'article'   == type ? include( '_config/article.variant.json' ) : null -}}


{## [Type] Collab → http://localhost:8000/cms/settings/entry-types/27
{-------------------------------------------------------------------------------------##}
{{- 'collab'    == type ? include( '_config/collab.variant.json' ) : null -}}


{## [EntryType] Matrix Builder Blocks
{---------------------------------------------------------------------------------------}
    Card Deck → http://localhost:8000/cms/settings/entry-types/5
    Component → http://localhost:8000/cms/settings/entry-types/7
    Fragment  → http://localhost:8000/cms/settings/entry-types/8
    Image     → http://localhost:8000/cms/settings/entry-types/3
    Media     → http://localhost:8000/cms/settings/entry-types/6
    Text      → http://localhost:8000/cms/settings/entry-types/4
{-------------------------------------------------------------------------------------##}
{{- 'text'      == type ? include( '_config/block.text.json'      ) : null -}}
{{- 'image'     == type ? include( '_config/block.image.json'     ) : null -}}
{{- 'media'     == type ? include( '_config/block.media.json'     ) : null -}}
{{- 'carddeck'  == type ? include( '_config/block.carddeck.json'  ) : null -}}
{{- 'component' == type ? include( '_config/block.component.json' ) : null -}}
{{- 'fragment'  == type ? include( '_config/block.fragment.json'  ) : null -}}


{## [EntryType] Related Resources
{---------------------------------------------------------------------------------------}
    Audio     → http://localhost:8000/cms/settings/entry-types/37
    Knowledge → http://localhost:8000/cms/settings/entry-types/36
    Download  → http://localhost:8000/cms/settings/entry-types/38
{-------------------------------------------------------------------------------------##}
{{- 'audio'     == type ? include( '_config/resource.audio.json'     ) : null -}}
{{- 'knowledge' == type ? include( '_config/resource.knowledge.json' ) : null -}}
{{- 'download'  == type ? include( '_config/resource.download.json'  ) : null -}}


{## [EntryType] Person → http://localhost:8000/cms/settings/entry-types/32
{-------------------------------------------------------------------------------------##}
{{- 'person'    == type ? include( '_config/person.variant.json' ) : null -}}


{## [EntryType] Session → http://localhost:8000/cms/settings/entry-types/35
{-------------------------------------------------------------------------------------##}
{{- 'session'   == type ? include( '_config/session.variant.json' ) : null -}}


{## [EntryType] Package → http://localhost:8000/cms/settings/entry-types/23
{-------------------------------------------------------------------------------------##}
{{- 'package'   == type ? include( '_config/package.variant.json' ) : null -}}


{## [EntryType] Timeslot → http://localhost:8000/cms/settings/entry-types/21
{-------------------------------------------------------------------------------------##}
{{- 'timeslot'  == type ? include( '_config/timeslot.variant.json' ) : null -}}


{## [EntryType] Brand → http://localhost:8000/cms/settings/entry-types/28
{-------------------------------------------------------------------------------------##}
{{- 'brand'     == type ? include( '_config/brand.variant.json' ) : null -}}


{## [EntryType] Venue → http://localhost:8000/cms/settings/entry-types/30
{-------------------------------------------------------------------------------------##}
{{- 'venue'     == type ? include( '_config/venue.variant.json' ) : null -}}


{## [EntryType] City → http://localhost:8000/cms/settings/entry-types/29
{-------------------------------------------------------------------------------------##}
{{- 'city'      == type ? include( '_config/city.variant.json' ) : null -}}


{## [Asset] Static Volume → http://localhost:8000/cms/settings/assets/volumes/2
{-------------------------------------------------------------------------------------##}
{{- 'static'    == type ? include( '_config/asset.static.json' ) : null -}}


{## All possible options ##}
{% if not type %}
    {% set combined =       [] | merge( include( '_config/block.text.json'       ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/block.image.json'      ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/block.media.json'      ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/block.carddeck.json'   ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/block.component.json'  ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/block.fragment.json'   ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/addon.variant.json'    ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/article.variant.json'  ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/collab.variant.json'   ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/person.variant.json'   ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/session.variant.json'  ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/timeslot.variant.json' ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/package.variant.json'  ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/brand.variant.json'    ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/venue.variant.json'    ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/city.variant.json'     ) | json_decode ) %}
    {% set combined = combined | merge( include( '_config/asset.static.json'     ) | json_decode ) %}
    {{ combined | json_encode }}
{% endif %}