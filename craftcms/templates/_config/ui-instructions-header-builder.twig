<div class="fieldTemplate fieldTemplate__headerBuilder">
    <h2>Header Builder</h2>

    <p>
        Use the header builder to introduce the content on the page, highlight vital information, solicit
        conversions, or to tease the content that appears below the fold.
    </p>

    <p>
        If you don't customize the header section, the entry title (above) will be used.

        <a href="/sitehub/blocks/header" target="_blank">Header&nbsp;builder&nbsp;reference&nbsp;→</a>
    </p>
</div>

{% css %}
    {# #fields-headerBuilder .actions a.move.icon {
        display: none;
    } #}
{% endcss %}


{% js %}

{# // provide a better message when a page already has a header block
(function($) {
    $(document).on('click', '#noMoreHerosText span[data-icon="settings"]', function(e) {
        $(this)
            .closest('div.matrix-field')
            .find( 'div.actions button.settings' )
            [ 0 ].click()
    });
})(jQuery); #}

{% endjs %}