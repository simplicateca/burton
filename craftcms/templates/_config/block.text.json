[{
    "label": "Single Text",
    "value": "single",
    "docs": {
        "tooltip": "The simplest and most humble of all variants, this is a single text block."
    },
    "inputs": [
        {
            "label": "Primary Copy Format",
            "field": "textlayer",
            "type" : "select",
            "default": "default",
            "instructions": "Typography treatment for the Primary Copy",
            "options" : {
                "default"   : "Default",
                "default2col"  : "Two Columns (Auto)"
            }
        }
    ],
    "settings": {}
},{
    "label": "Text (with Secondary Reveal)",
    "value": "reveal",
    "docs": {
        "tooltip": "A single text block with an interactive reveal for secondary content."
    },
    "inputs": [
        {
            "label": "Primary Copy Format",
            "field": "textlayer",
            "type" : "select",
            "instructions": "Typography treatment for the Primary Copy",
            "default": "default",
            "options" : {
                "default": "Default Treatment"
            }
        }
       ,{
            "label": "Secondary Copy Format",
            "field": "text2layer",
            "type" : "select",
            "instructions": "Typography treatment for the Secondary Copy",
            "default": "default",
            "options" : {
                "default": "Default Treatment",
                "default__boxout": "Box Knockout"
            }
        }
       ,{
            "label": "Revealer Button Label",
            "field": "revealLabel",
            "type" : "text",
            "default": "Continue Reading..."
        }
    ],
    "settings": {}
},{
    "label": "Text with Callout",
    "value": "callout",
    "docs": {
        "tooltip": "A single text block with an aside."
    },
    "inputs": [
        {
            "label": "Primary Copy Format",
            "field": "textlayer",
            "type" : "select",
            "default": "default",
            "instructions": "Typography treatment for the Primary Copy",
            "options" : {
                "default"   : "Default"
            }
        }
       ,{
            "label": "Callout Format",
            "field": "text2layer",
            "type" : "select",
            "instructions": "Typography treatment for the Callout",
            "options" : {
                "callout": "Default",
                "callout__quote": "Quote",
                "callout__info" : "Information",
                "callout__warn" : "Warning"
            }
        }
    ],
    "settings": {}
},{
    "label": "Double Text (Side-by-Side)",
    "value": "double",
    "docs": {
        "tooltip": "Two text blocks, side-by-side."
    },
    "inputs": [
        {
            "label": "Primary Copy Format",
            "field": "textlayer",
            "type" : "select",
            "instructions": "Typography treatment for the Primary Copy",
            "default": "default",
            "options" : {
                "default": "Default Treatment"
            }
        }
       ,{
            "label": "Secondary Copy Format",
            "field": "text2layer",
            "type" : "select",
            "instructions": "Typography treatment for the Secondary Copy",
            "default": "default",
            "options" : {
                "default": "Default Treatment",
                "default__boxout": "Box Knockout"
            }
        }
    ],
    "settings": {}
}]
