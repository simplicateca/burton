[{
    "value": "single",
    "label": "Single Text",
    "tooltips": {
        "inline": "The simplest and most humble of all variants, this is a single text block."
    },
    "settings": {},
    "virtuals": [{
        "type" : "select",
        "name" : "textlayer",
        "default": "default",
        "label": "Typography",
        "instructions": "Choose the base `Copy` format.",
        "options" : {
            "default": "Default",
            "twocols": "Two Columns"
        }
    }]
},{
    "value": "reveal",
    "label": "Text (with Secondary Reveal)",
    "tooltips": {
        "inline": "A single text block with an interactive reveal for secondary content."
    },
    "settings": {},
    "virtuals": [{
        "type" : "select",
        "name" : "textlayer",
        "default": "default",
        "label": "Primary Copy Format",
        "instructions": "Typography treatment for the Primary Copy",
        "options" : {
            "default": "Default Text Treatment"
        }
    },{
        "type" : "select",
        "name" : "text2layer",
        "default": "default",
        "label": "Secondary Copy Format",
        "instructions": "Typography treatment for the Secondary Copy",
        "options" : {
            "default": "Default Treatment",
            "boxout" : "Box Knockout"
        }
    },{
        "type" : "text",
        "name" : "buttontext",
        "default": "Continue Reading...",
        "label": "Reveal Button Text"
    }]
},{
    "value": "callout",
    "label": "Text with Callout",
    "tooltips": {
        "inline": "A single text block with an aside."
    },
    "settings": {},
    "virtuals": [
        {
            "type" : "select",
            "name" : "textlayer",
            "default": "default",
            "label": "Primary Copy Format",
            "instructions": "Typography treatment for the Primary Copy",
            "options" : {
                "default": "Default Text Treatment"
            }
        }
       ,{
            "type" : "select",
            "name" : "text2layer",
            "default": "callout",
            "label": "Callout Format",
            "instructions": "Typography treatment for the Callout",
            "options" : {
                "callout": "Default",
                "callout__quote": "Quote"
            }
        }
    ]
},{
    "value": "double",
    "label": "Double Text",
    "tooltips": {
        "inline": "Two text blocks, side-by-side."
    },
    "settings": {},
    "virtuals": [
        {
            "type" : "select",
            "name" : "textlayer",
            "default": "default",
            "label": "Primary Copy Format",
            "instructions": "Typography treatment for the Primary Copy",
            "options" : {
                "default": "Default Text Treatment",
                "larger" : "Larger Text",
                "smaller": "Smaller Text",
                "callout": "Reverse / Knockout Text"
            }
        }
       ,{
            "type" : "select",
            "name" : "text2layer",
            "default": "default",
            "label": "Secondary Copy Format",
            "instructions": "Typography treatment for the Secondary Copy",
            "options" : {
                "default": "Default Text Treatment",
                "larger" : "Larger Text",
                "smaller": "Smaller Text",
                "callout": "Reverse / Knockout Text"
            }
        }
    ]
}]
