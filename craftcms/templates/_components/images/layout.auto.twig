{% extends "_components/images/_base" %}

{% block image %}

    {% set images = images ??? null %}
    {% set frame  = frame  ??? 'auto' %}

    {% if ( images ??? null ) and images|length > 1 %}
        {% embed "_components/images/layout.carousel"
           with {
            images: images,
            frame: frame,
            settings: settings ??? null
        } %}{% endembed %}
    {% else %}
        {% from "_components/images" import frame as framer %}
        {{ framer( images|first ??? null, frame, settings ??? null ) }}
    {% endif %}

{% endblock %}